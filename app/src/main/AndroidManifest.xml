<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitBuddy">

        <activity
            android:name="com.example.fit_buddy.activities.DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.fit_buddy.activities.DiaryActivity"
            android:exported="false"
            android:theme="@style/Theme.FitBuddy"/>
        <activity
            android:name="com.example.fit_buddy.activities.StatsActivity"
            android:exported="false"
            android:theme="@style/Theme.FitBuddy"/>

        <activity
            android:name="com.example.fit_buddy.activities.MoreActivity"
            android:exported="false"
            android:theme="@style/Theme.FitBuddy"/>
        <activity
            android:name="com.example.fit_buddy.activities.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.FitBuddy"/>
    </application>

</manifest>